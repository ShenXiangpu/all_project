---
description: 不懂就问、只改相关代码、不随意优化、改动尽量精简
alwaysApply: true
---

# 开发纪律

## 1. 不懂就问，不要自己想象

- 需求、接口字段、业务规则、环境配置等**不确定时，先向用户确认**，不要凭猜测实现。
- 不要虚构不存在的 API、文件路径、环境变量或「项目惯例」。
- 能从代码或文档里**查证**的，先查再动手；查不到且影响实现时，**明确列出待确认点**再问。

```
❌ 用户没说要缓存，我假设需要加 Redis
✅ 「列表是否需要分页？接口文档里没看到 page 参数，请确认。」
```

## 2. 不要动不相关的代码

- **只改完成任务所必需的文件和行**；不顺手改格式、重命名、挪 import、删注释。
- 同一文件里若必须改，**范围紧贴本次需求**，不与周围无关逻辑混在一起。
- 发现别处有 bug 或坏味道：**说明即可**，除非用户明确要求一并处理。

## 3. 不要随便优化

- 不以「顺便更好」为由做重构、抽公共组件、换库、性能微调、全文件格式化。
- **用户没提的优化不做**；若确有更好做法且能明显降低风险，**先说明利弊，等确认再改**。
- 优先沿用现有抽象与风格，不引入新架构或新模式。

## 4. 能用 10 行就不要写 20 行

- 选**最短、最直接**的实现路径；避免多余变量、包装函数、过度抽象和防御性分支。
- 不堆注释解释显而易见的事；不为「以后可能用到」预留未使用代码。
- 重复逻辑**仅在本次任务需要复用时**才抽取，不为抽而抽。
