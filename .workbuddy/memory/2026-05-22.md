# 2026-05-22

## CRM-Service 单元测试
- 为 authController login 编写了完整的 vitest 单元测试（5个用例），覆盖参数校验、用户不存在、成功登录三个分支
- 测试文件：`src/utils/_test/auth.test.ts`
- 核心修复：断言 message 从错误的 `'请输入用户名和密码'` 改为正确的 `'用户名和密码不能为空'`；增加了 model/bcrypt/jwt 的 vi.mock 隔离外部依赖
- 5 个测试全部通过

## crm-new-project 对接 crm-service 后端
- 完成前端从 Mock 模式切换为对接真实后端（Express + TypeScript + JWT）
- 改动文件：
  - `vite.config.js`: 移除 vite-plugin-mock，添加 proxy `/api` → `http://localhost:3000`，前端端口改为 5173（避免与后端 3000 冲突）
  - `src/utils/request.js`: 认证头从 `Auth-token` 改为 `auth-token`（匹配后端），新增 401 响应拦截自动清除 token 并跳转登录页
  - `src/views/login/index.vue`: 移除"测试账号: admin / admin123"提示文字及样式
  - `package.json`: 移除 vite-plugin-mock，保留 mockjs（测试文件依赖）
- 验证：4个测试文件 46 个用例全部通过，Vite 开发服务器在 5173 端口正常启动
- 使用方式：先启动 crm-service 后端（端口 3000），再启动前端 `npm run dev`，登录时用数据库中的真实账号密码
